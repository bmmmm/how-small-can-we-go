# built harness binary
/arena
/tool/tool

# generated board (built in CI, deployed to Pages)
/docs/

# local scratch (drafts, throwaway experiments) and agent worktrees
/tmp/
/.claude/worktrees/

# personal Claude Code overrides (host-specific paths, never committed)
/.claude/settings.local.json
